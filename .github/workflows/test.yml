name: Validate and build

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate JSON contracts, configuration, fixtures, and public data
        run: python scripts/validate_data_contract.py

      - name: Check private-sector study regression cases
        run: python -m unittest discover -s tests -p test_private_sector_study.py

      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: site/pnpm-lock.yaml

      - name: Install site dependencies
        working-directory: site
        run: pnpm install --frozen-lockfile

      - name: Type-check and build static site
        working-directory: site
        run: pnpm run build
